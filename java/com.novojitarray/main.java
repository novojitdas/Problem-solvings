package com.novojitarray;

import java.util.Arrays;

public class Main {

    public static void main(String[] args) {
        Array numbers = new Array(4);
        numbers.insert(10);
        numbers.insert(20);
        numbers.insert(30);
        numbers.reverse();

        // System.out.println(numbers.max()); // find the maximum number (max method) //
        // numbers.print(); // print method of array elements //
        // System.out.println(numbers.length); // print the value of array length //

    }
}
